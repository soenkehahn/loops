name: loop
ghc-options:
  - -fdefer-typed-holes
  # - -fdefer-type-errors
  - -O1

dependencies:
  - base
  - random
  - bytestring
  - bytestring-conversion

executables:
  loop:
    main: main.hs
    source-dirs:
      - src

tests:
  spec:
    main: Spec.hs
    source-dirs:
      - test
      - src
    dependencies:
      - hspec
      - HUnit
      - string-conversions
