name: song
ghc-options:
  - -fdefer-typed-holes
  - -O1
  - -Wall
  - -Wno-name-shadowing
default-extensions:
  - Strict

dependencies:
  - base
  - random
  - transformers
  - loops-lib

executables:
  song:
    main: main.hs
    source-dirs:
      - src
